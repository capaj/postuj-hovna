var expect = require('chai').expect;
var Map = require('../components/map');

describe('map', function() {
    it.skip('should have property for storing the current markers on the map', function(){
    });
});